import { ConfirmFieldsDirective } from './confirm-fields.directive';

describe('ConfirmFieldsDirective', () => {
  it('should create an instance', () => {
    const directive = new ConfirmFieldsDirective();
    expect(directive).toBeTruthy();
  });
});
